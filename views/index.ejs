<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="main-w-full min-h-screen bg-zinc-900">
      <div class="form p-10">
        <form action="/create" method="post">
          <input
            class="block rounded bg-zinc-800 mb-5 w-full px-3 text-white"
            type="text"
            placeholder="title"
            name="title"
          />
          <textarea
            class="block rounded bg-zinc-800 px-3 w-full resize-none text-white"
            placeholder="enter text here"
            name="details"
          ></textarea>
          <input
            type="submit"
            value="create task"
            class="bg-green-400 rounded mt-5 py-2 px-5"
          />
        </form>
      </div>
      <div class="tasks flex flex-clos gap-3 flex-wrap p-10">
        <%if(files.length>0){ %> <%files.forEach(function(val){ %>
        <div class="task w-72 px-3 py-4 bg-zinc-800 rounded-md">
          <h1 class="text-white text-2xl tracking-tight"><%=val %></h1>
          <div class="flex justify-between">
            <a class="text-blue-500 inline-block mt-3" href="/files/<%= val%>">Read more.</a>
            <a class="text-blue-500 inline-block mt-3" href="/edit/<%= val%>">Edit Filename</a>
          </div>
        </div>
        <% }) %> <% } else{%>
        <h3 class="text-2xl text-blue-500 ml-5">No task here....</h3>

        <%}%>
      </div>
    </div>
  </body>
</html>
