<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-800 p-10">
        <a href="/" class="text-blue-500">Back to home..</a>
        <br> <br>
        <h1 class="text-3xl text-white">All User</h1>
        <br>
        <div class="users flex gap-11 flex-wrap">
            <% if (users.length > 0) { %>
                <% users.forEach(function(user) { %>
                    <div class="user w-72 p-4 bg-zinc-700 rounded-lg">
                        <div class="w-full h-52 bg-zinc-500 rounded-lg overflow-hidden">
                            <img class="w-full h-full object-cover" src="<%= user.image %>" alt="">
                        </div>
                        <h3 class="text-xl mt-3 tracking-tighter"><%= user.name %></h3>
                        <h5 class="text-zinc-500"><%= user.email %></h5>
                        <div class="flex gap-3">
                            <a href="/edit/<%= user._id%>" class="text-white">Edit User</a>
                            <a href="/delete/<%= user._id %>" class="text-red-400">Delete User</a>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <h2 class="text-white">No users yet...</h2>
            <% } %>
        </div>
    </div>
</body>
</html>
